const inputs = document.querySelectorAll('.inputs');
const labels = document.querySelectorAll('#check');
// console.log(labels);

inputs.forEach(input=>{
    input.addEventListener('click',()=>{
        // console.log('hi');
        
    });
});